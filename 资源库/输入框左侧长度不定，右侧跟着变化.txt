效果：
 上海：输入框

但是前面的上海这个文字不是固定的，后面的输入框要占用剩下的所有空间，而且要跟着前面的上海的文字的变化而变化；

做法：
<p>
	<a href="跳到上海">上海</a>
	<a href="调到输入框输入页">模拟的输入框</a>
</p>

让左侧的a标签float：left;让右侧的a标签display：block;
这样就行了。这样的话右侧的会占去整一行，但是由于左侧
的是浮动的所以右侧的虽然占取了一行，但是不会换行的。同时，由于浮动元素的后面（背后）永远是空白的（这是浮动元素的一个特性）；
利用这一点就可以做到左侧的一直在左侧，而且左侧的层级要高于右侧，同时右侧的会自动的占取剩余的所有空间。 哈哈哈！利用这一点
如果以后还不能发现bug的话都可以用来代替弹性布局了。