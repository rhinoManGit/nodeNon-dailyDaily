1. 在页面中有一段文字到达了边框的时候，

如果是中文字符的话他们会自动换行；

但是如果是一段英文字符的时候他们会默认为一个单词，从而不会换行，要不都换到第二行，要不都不换，
如果一定想要换行的话可以用css3的属性word-break属性强制做。


2. write-space:是指一行文本要不要换行，css1的

3. text-overflow:css3的属性
clip：
当对象内文本溢出时不显示省略标记（...），而是将溢出的部分裁切掉。
ellipsis：
当对象内文本溢出时显示省略标记（...）。